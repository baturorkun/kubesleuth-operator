---
apiVersion: v1
kind: Service
metadata:
  name: controller-manager-dashboard-service
  namespace: system
  labels:
    control-plane: controller-manager
    app.kubernetes.io/name: kubebuilder-demo-operator
    app.kubernetes.io/managed-by: kustomize
spec:
  type: ClusterIP
  ports:
  - port: 8082
    protocol: TCP
    name: dashboard
    targetPort: dashboard
  selector:
    control-plane: controller-manager
    app.kubernetes.io/name: kubebuilder-demo-operator
