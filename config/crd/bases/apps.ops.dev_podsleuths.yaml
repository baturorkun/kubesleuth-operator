---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.19.0
  name: podsleuths.apps.ops.dev
spec:
  group: apps.ops.dev
  names:
    kind: PodSleuth
    listKind: PodSleuthList
    plural: podsleuths
    singular: podsleuth
  scope: Cluster
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: PodSleuth is the Schema for the podsleuths API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: spec defines the desired state of PodSleuth
            properties:
              logAnalysis:
                description: LogAnalysis enables log analysis for running but not
                  ready pods
                properties:
                  aiApiKey:
                    description: |-
                      AIAPIKey is the API key for AI analysis
                      Deprecated: Use MethodConfigs with AIConfig instead
                      Should be stored as a Kubernetes Secret reference
                    properties:
                      key:
                        description: The key of the secret to select from.  Must be
                          a valid secret key.
                        type: string
                      name:
                        default: ""
                        description: |-
                          Name of the referent.
                          This field is effectively required, but due to backwards compatibility is
                          allowed to be empty. Instances of this type with an empty value here are
                          almost certainly wrong.
                          More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                        type: string
                      optional:
                        description: Specify whether the Secret or its key must be
                          defined
                        type: boolean
                    required:
                    - key
                    type: object
                    x-kubernetes-map-type: atomic
                  aiAuthHeader:
                    description: |-
                      AIAuthHeader specifies the HTTP header name for authentication
                      Deprecated: Use MethodConfigs with AIConfig instead
                      Default: "Authorization"
                    type: string
                  aiAuthPrefix:
                    description: |-
                      AIAuthPrefix specifies the prefix for the auth header value (e.g., "Bearer", "ApiKey")
                      Deprecated: Use MethodConfigs with AIConfig instead
                      Default: "Bearer"
                    type: string
                  aiEndpoint:
                    description: |-
                      AIEndpoint is the URL endpoint for AI analysis (required if "ai" in methods)
                      Deprecated: Use MethodConfigs with AIConfig instead for cleaner structure
                      Examples:
                        - OpenAI: "https://api.openai.com/v1/chat/completions"
                        - Ollama: "http://localhost:11434/api/generate"
                    type: string
                  aiFormat:
                    description: |-
                      AIFormat specifies the API format to use: "openai", "anthropic", "ollama", or "generic"
                      Deprecated: Use MethodConfigs with AIConfig instead
                      Default: "openai"
                    type: string
                  aiModel:
                    description: |-
                      AIModel specifies the model name to use for AI analysis
                      Deprecated: Use MethodConfigs with AIConfig instead
                      Examples: "gpt-4", "qwen3:8b", "claude-3-opus"
                    type: string
                  cacheEnabled:
                    description: |-
                      CacheEnabled enables caching of analysis results to avoid re-analyzing on every reconcile
                      Results are cached per pod (keyed by UID + restart count)
                      Cache is invalidated when pod restarts or after TTL expires
                      Default: true
                    type: boolean
                  cacheTTL:
                    description: |-
                      CacheTTL is the duration to cache analysis results before re-analyzing
                      Default: 5m
                    type: string
                  enabled:
                    description: Enabled enables log analysis for non-ready pods
                    type: boolean
                  filterErrorsOnly:
                    description: |-
                      FilterErrorsOnly if true, filters error/warning lines from the last LinesToAnalyze lines
                      Process: 1) Fetch last LinesToAnalyze lines, 2) Filter for errors/warnings, 3) Analyze filtered lines
                      Default: true
                    type: boolean
                  linesToAnalyze:
                    description: |-
                      LinesToAnalyze is the number of recent log lines to fetch and analyze
                      Default: 100
                    format: int32
                    type: integer
                  method:
                    description: |-
                      Method specifies the analysis method: "pattern" or "ai"
                      Deprecated: Use Methods instead for multiple method support
                      Default: "pattern"
                    type: string
                  methodConfigs:
                    description: |-
                      MethodConfigs defines method-specific configurations in order of execution
                      This is the NEW PREFERRED way to configure log analysis methods.
                      Each method has its own configuration block, avoiding parameter mixing.
                      Example:
                        methodConfigs:
                          - type: "pattern"
                            patternConfig:
                              patterns: [...]
                          - type: "ai"
                            aiConfig:
                              endpoint: "..."
                              model: "..."
                    items:
                      description: MethodConfig defines configuration for a specific
                        analysis method
                      properties:
                        aiConfig:
                          description: AIConfig contains AI-specific configuration
                            (used when type is "ai")
                          properties:
                            apiKeySecretRef:
                              description: APIKeySecretRef references a secret containing
                                the API key
                              properties:
                                key:
                                  description: The key of the secret to select from.  Must
                                    be a valid secret key.
                                  type: string
                                name:
                                  default: ""
                                  description: |-
                                    Name of the referent.
                                    This field is effectively required, but due to backwards compatibility is
                                    allowed to be empty. Instances of this type with an empty value here are
                                    almost certainly wrong.
                                    More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                                  type: string
                                optional:
                                  description: Specify whether the Secret or its key
                                    must be defined
                                  type: boolean
                              required:
                              - key
                              type: object
                              x-kubernetes-map-type: atomic
                            authHeader:
                              description: |-
                                AuthHeader specifies the HTTP header name for authentication
                                Default: "Authorization"
                              type: string
                            authPrefix:
                              description: |-
                                AuthPrefix specifies the prefix for the auth header value
                                Default: "Bearer"
                              type: string
                            endpoint:
                              description: |-
                                Endpoint is the URL endpoint for AI analysis
                                Examples:
                                  - OpenAI: "https://api.openai.com/v1/chat/completions"
                                  - Ollama: "http://localhost:11434/api/generate"
                              type: string
                            format:
                              description: |-
                                Format specifies the API format: "openai", "anthropic", "ollama", or "generic"
                                Default: "openai"
                              type: string
                            model:
                              description: |-
                                Model specifies the model name to use
                                Examples: "gpt-4", "qwen3:8b", "claude-3-opus"
                              type: string
                          required:
                          - endpoint
                          type: object
                        patternConfig:
                          description: PatternConfig contains pattern-specific configuration
                            (used when type is "pattern")
                          properties:
                            patterns:
                              description: |-
                                Patterns defines custom error patterns for pattern matching
                                If not specified, default patterns will be used
                              items:
                                description: ErrorPattern defines a pattern to match
                                  error messages in logs
                                properties:
                                  name:
                                    description: Name is a descriptive name for this
                                      pattern (e.g., "KafkaConnectionError")
                                    type: string
                                  pattern:
                                    description: Pattern is the regex pattern to match
                                      against log lines
                                    type: string
                                  priority:
                                    description: |-
                                      Priority determines which pattern to use if multiple patterns match
                                      Higher priority patterns are preferred. Default: 0
                                    format: int32
                                    type: integer
                                  rootCause:
                                    description: |-
                                      RootCause is the root cause message to report when this pattern matches
                                      If empty, the matched log line will be used as the root cause
                                    type: string
                                required:
                                - name
                                - pattern
                                type: object
                              type: array
                          type: object
                        type:
                          description: 'Type specifies the analysis method type: "pattern"
                            or "ai"'
                          enum:
                          - pattern
                          - ai
                          type: string
                      required:
                      - type
                      type: object
                    type: array
                  methods:
                    description: |-
                      Methods specifies the analysis methods to run in order (e.g., ["pattern", "ai"])
                      Methods are executed sequentially in the order specified
                      Example: ["pattern", "ai"] runs pattern first, then AI
                      Default: ["pattern"]
                    items:
                      type: string
                    type: array
                  patterns:
                    description: |-
                      Patterns defines custom error patterns for pattern matching method
                      Deprecated: Use MethodConfigs with PatternConfig instead for cleaner structure
                      If not specified, default patterns will be used (connection errors, service unavailable, etc.)
                      Each pattern should be a regex that matches error messages
                    items:
                      description: ErrorPattern defines a pattern to match error messages
                        in logs
                      properties:
                        name:
                          description: Name is a descriptive name for this pattern
                            (e.g., "KafkaConnectionError")
                          type: string
                        pattern:
                          description: Pattern is the regex pattern to match against
                            log lines
                          type: string
                        priority:
                          description: |-
                            Priority determines which pattern to use if multiple patterns match
                            Higher priority patterns are preferred. Default: 0
                          format: int32
                          type: integer
                        rootCause:
                          description: |-
                            RootCause is the root cause message to report when this pattern matches
                            If empty, the matched log line will be used as the root cause
                          type: string
                      required:
                      - name
                      - pattern
                      type: object
                    type: array
                required:
                - enabled
                type: object
              podLabelSelector:
                description: |-
                  PodLabelSelector is a label selector to filter pods across all namespaces.
                  If not specified, monitors all pods in all namespaces.
                properties:
                  matchExpressions:
                    description: matchExpressions is a list of label selector requirements.
                      The requirements are ANDed.
                    items:
                      description: |-
                        A label selector requirement is a selector that contains values, a key, and an operator that
                        relates the key and values.
                      properties:
                        key:
                          description: key is the label key that the selector applies
                            to.
                          type: string
                        operator:
                          description: |-
                            operator represents a key's relationship to a set of values.
                            Valid operators are In, NotIn, Exists and DoesNotExist.
                          type: string
                        values:
                          description: |-
                            values is an array of string values. If the operator is In or NotIn,
                            the values array must be non-empty. If the operator is Exists or DoesNotExist,
                            the values array must be empty. This array is replaced during a strategic
                            merge patch.
                          items:
                            type: string
                          type: array
                          x-kubernetes-list-type: atomic
                      required:
                      - key
                      - operator
                      type: object
                    type: array
                    x-kubernetes-list-type: atomic
                  matchLabels:
                    additionalProperties:
                      type: string
                    description: |-
                      matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels
                      map is equivalent to an element of matchExpressions, whose key field is "key", the
                      operator is "In", and the values array contains only "value". The requirements are ANDed.
                    type: object
                type: object
                x-kubernetes-map-type: atomic
              reconcileInterval:
                description: |-
                  ReconcileInterval is the duration for periodic reconciliation.
                  Default: 5 minutes
                type: string
            type: object
          status:
            description: status defines the observed state of PodSleuth
            properties:
              conditions:
                description: |-
                  conditions represent the current state of the PodSleuth resource.
                  Each condition has a unique type and reflects the status of a specific aspect of the resource.

                  Standard condition types include:
                  - "Available": the resource is fully functional
                  - "Progressing": the resource is being created or updated
                  - "Degraded": the resource failed to reach or maintain its desired state

                  The status of each condition is one of True, False, or Unknown.
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              nonReadyPods:
                description: NonReadyPods is a dynamic list of non-ready pods
                items:
                  description: NonReadyPodInfo contains information about a non-ready
                    pod
                  properties:
                    containerErrors:
                      description: ContainerErrors contains detailed error information
                        for each unready container
                      items:
                        description: ContainerError contains detailed error information
                          for a specific container
                        properties:
                          containerName:
                            description: ContainerName is the name of the container
                            type: string
                          exitCode:
                            description: ExitCode is the exit code if the container
                              terminated
                            format: int32
                            type: integer
                          message:
                            description: Message is the detailed error message
                            type: string
                          ready:
                            description: Ready indicates if the container is ready
                            type: boolean
                          reason:
                            description: Reason is the error reason (CrashLoopBackOff,
                              ImagePullBackOff, etc.)
                            type: string
                          restartCount:
                            description: RestartCount is the number of times the container
                              has restarted
                            format: int32
                            type: integer
                          state:
                            description: State is the current state of the container
                              (waiting, terminated, running)
                            type: string
                          type:
                            description: Type indicates whether this is a regular
                              container or init container
                            type: string
                        required:
                        - containerName
                        - message
                        - ready
                        - reason
                        - restartCount
                        - state
                        - type
                        type: object
                      type: array
                    logAnalysis:
                      description: LogAnalysis contains results from log analysis
                        if enabled
                      properties:
                        aiResult:
                          description: AIResult contains AI-specific analysis details
                          properties:
                            confidence:
                              description: Confidence is the confidence level (0-100)
                                from AI analysis
                              format: int32
                              type: integer
                            error:
                              description: Error contains any error message if AI
                                analysis failed
                              type: string
                            model:
                              description: Model is the AI model used for analysis
                              type: string
                            rootCause:
                              description: RootCause is the root cause identified
                                by AI
                              type: string
                          type: object
                        analyzedAt:
                          description: AnalyzedAt is when the analysis was performed
                          format: date-time
                          type: string
                        cachedAt:
                          description: CachedAt is when the result was cached (if
                            caching is enabled)
                          format: date-time
                          type: string
                        confidence:
                          description: Confidence is the confidence level (0-100)
                            of the analysis (merged from all methods)
                          format: int32
                          type: integer
                        errorLines:
                          description: ErrorLines contains the error lines that led
                            to this conclusion
                          items:
                            type: string
                          type: array
                        matchedPattern:
                          description: |-
                            MatchedPattern is the name of the pattern that matched (for pattern analysis)
                            Used internally, prefer PatternResult.MatchedPattern
                          type: string
                        method:
                          description: |-
                            Method used for analysis: "pattern" or "ai"
                            Deprecated: Use Methods instead for multiple method support
                          type: string
                        methods:
                          description: Methods used for analysis in execution order
                            (e.g., ["pattern", "ai"])
                          items:
                            type: string
                          type: array
                        model:
                          description: |-
                            Model is the AI model used (for AI analysis)
                            Used internally, prefer AIResult.Model
                          type: string
                        patternResult:
                          description: PatternResult contains pattern-specific analysis
                            details
                          properties:
                            confidence:
                              description: Confidence is the confidence level (0-100)
                                of the pattern match
                              format: int32
                              type: integer
                            error:
                              description: Error contains any error message if pattern
                                analysis failed
                              type: string
                            matchedPattern:
                              description: MatchedPattern is the name of the pattern
                                that matched
                              type: string
                            priority:
                              description: Priority is the priority of the matched
                                pattern
                              format: int32
                              type: integer
                            rootCause:
                              description: RootCause is the root cause from pattern
                                matching
                              type: string
                          type: object
                        priority:
                          description: |-
                            Priority is the priority of the matched pattern (for pattern analysis)
                            Used internally, prefer PatternResult.Priority
                          format: int32
                          type: integer
                        rootCause:
                          description: RootCause is the identified root cause from
                            log analysis (merged from all methods)
                          type: string
                      type: object
                    message:
                      description: Message is the detailed message explaining why
                        the pod is not ready
                      type: string
                    name:
                      description: Name is the name of the pod
                      type: string
                    namespace:
                      description: Namespace is the namespace of the pod
                      type: string
                    ownerKind:
                      description: OwnerKind is the kind of the owner (Deployment
                        or StatefulSet)
                      type: string
                    ownerName:
                      description: OwnerName is the name of the owner
                      type: string
                    phase:
                      description: Phase is the current phase of the pod (Pending,
                        Running, Failed, etc.)
                      type: string
                    podConditions:
                      description: PodConditions contains all pod conditions for comprehensive
                        status
                      items:
                        description: PodCondition represents a pod condition status
                        properties:
                          message:
                            description: Message is the message describing the condition
                            type: string
                          reason:
                            description: Reason is the reason for the condition status
                            type: string
                          status:
                            description: Status is the status of the condition (True,
                              False, Unknown)
                            type: string
                          type:
                            description: Type is the type of condition
                            type: string
                        required:
                        - status
                        - type
                        type: object
                      type: array
                    reason:
                      description: Reason is the primary reason why the pod is not
                        ready (from container status investigation)
                      type: string
                  required:
                  - name
                  - namespace
                  - phase
                  type: object
                type: array
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
